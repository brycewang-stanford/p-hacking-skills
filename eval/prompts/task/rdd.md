## Task — regression discontinuity

We observe a running variable that mechanically determines treatment at a
threshold, and an outcome measured afterwards. Implement a regression
discontinuity design to estimate the causal effect of crossing the threshold.

Data: `{data}`

- Running variable: `{running}`, centred so 0 is the threshold
- Outcome: `{outcome}`
- Identifier: `{id}`
